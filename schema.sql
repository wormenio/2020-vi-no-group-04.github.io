
    create table Documentos_comerciales (
        id integer generated by default as identity (start with 1),
        nombre varchar(255),
        tipo_documento_comercial_id integer,
        primary key (id)
    )

    create table Items (
        id integer generated by default as identity (start with 1),
        nombre varchar(255),
        primary key (id)
    )

    create table Proveedores (
        id integer generated by default as identity (start with 1),
        apellido varchar(255),
        codigo_postal varchar(255),
        cuit varchar(255),
        dni varchar(255),
        nombre varchar(255),
        razon_social varchar(255),
        documento_comercial_id integer not null,
        fecha varbinary(255),
        medio_de_pago_id integer not null,
        medio_de_pago_nro integer not null,
        monto_total double,
        proveedor_id integer not null,
        primary key (id)
    )

    create table Tipo_documento_comercial (
        id integer generated by default as identity (start with 1),
        nombre varchar(255),
        primary key (id)
    )

    alter table Documentos_comerciales 
        add constraint FK_aeey1ct6gl1ix21u3o394neip 
        foreign key (tipo_documento_comercial_id) 
        references Tipo_documento_comercial
